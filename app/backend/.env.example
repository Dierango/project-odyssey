# Environment Variables for Google Cloud Run
# Set these in the Google Cloud Console or via gcloud command

# Database (you'll need to set up Cloud SQL or use a free PostgreSQL service)
DATABASE_URL=postgresql://user:password@host:port/dbname

# JWT Secret
SECRET_KEY=your-super-secret-jwt-key

# Google AI API Key (if using Gemini)
GOOGLE_API_KEY=your-google-ai-api-key

# Example gcloud command to set environment variables:
# gcloud run services update project-odyssey-backend \
#   --region=us-central1 \
#   --set-env-vars="DATABASE_URL=your-db-url,SECRET_KEY=your-secret,GOOGLE_API_KEY=your-api-key"
